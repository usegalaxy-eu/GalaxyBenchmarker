benchmarks:
  verify_setup_benchmark:
    type: PosixSetupTimeBenchmark
    repetitions: 3

    pre_tasks:
      - playbook: install_docker.yml
        host: "irods_client,irods_server"

    hosts:
      - irods_client
      - irods_server
